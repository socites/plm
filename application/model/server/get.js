module.exports = require('async')(function* (resolve, reject, params, context) {
    "use strict";

    //resolve(yield  trivias.get(params));
    return [
        {'1': '000000'},
        {'2': '000000'},
        {'3': '000000'}
    ]

});
